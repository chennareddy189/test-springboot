apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-spring
  labels:
    app: simple-spring
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-spring
  template:
    metadata:
      labels:
        app: simple-spring
    spec:
      # If your image is in a private registry, create a docker-registry secret
      # and set it here as `imagePullSecrets` (example README shows how).
      imagePullSecrets:
      - name: regcred
      containers:
      - name: simple-spring
        # REPLACE this image with your registry and repo (example: ghcr.io/OWNER/simple-spring-cicd:latest)
        image: ghcr.io/chennareddy189/simple-spring-cicd:latest
        ports:
        - containerPort: 8080
        # The application exposes /health (controller) not /actuator/health by default.
        # Use /health for readiness and liveness so probes succeed. If you enable
        # Spring Actuator later, you can switch back to /actuator/health.
        # Use Spring Boot Actuator health endpoint for probes. Increased
        # initial delays to give the JVM time to start before probes run.
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: default

